# Archived backend experiments

These scripts were historical stress, benchmark, nonlinear, and demonstration
experiments found at the backend package root during repository cleanup. They
are preserved for provenance but are not part of the maintained application,
are not imported by the API, and must not be used as validation evidence.

Maintained tests live under `app/backend/tests/` and maintained engineering
modules live under `app/backend/engineering/`.