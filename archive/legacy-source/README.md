# Legacy source archive

This directory contains source-only snapshots of the former DesignBook v1 and
v2 trees. Generated logs, installed dependencies, virtual environments, build
outputs, secrets, and generated PDFs were intentionally excluded. The archive
is retained because the workspace had no Git history when cleanup occurred; it
is not part of the application runtime or deployment path.
